class Solution {
public:
    int search(vector<int>& nums, int target) {
           int low = 0;
           int high = nums.size() - 1;

           while(low <= high){
                   
            int mid = (low + high)/2;

            if(nums[mid] == target){
                return mid;
            }
            if(nums[mid] > target){
                high = (mid - 1);
            }
            if(nums[mid] < target){
                low = (mid + 1);
            }
           
           }

           return -1;
    }
};

Competitive coding / production code / large input → Iterative
Concept clarity / academic explanation / small input → Recursive

✅ Iterative Binary Search generally better hota hai:

-> Kyunki space efficient hai (O(1) extra space)

-> Recursion ka function call stack overhead nahi hota

-> Practically faster hota hai
