Identity Matrix mai sirf primary diagonal (i,i) == 0 rest other elements are non-zero
should be a square matrix only

bool identity(vector<vector<int>>&arr, int rows, int cols){
    if(rows != cols) return false;

    for(int i = 0; i < rows; i++){
        if(arr[i][i] != 1) return false;

        for(int j = 0; j < cols; j++){
            if(i == j) continue;
            if(arr[i][j] != 0) return false;
        }
    }
    return true;
}
